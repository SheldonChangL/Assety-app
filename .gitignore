*.iml
.gradle
/local.properties
local.properties
.DS_Store
/build
/captures
.externalNativeBuild
.cxx

# IDE — exclude entire .idea directory (machine-local, volatile)
/.idea/

# Release signing artifacts — NEVER commit to VCS
release.keystore
keystore_secrets.txt

# Compiled app artifacts — upload via Play Console, not VCS
*.aab
*.apk
/app/release/

# Claude Code local session data
/.claude/
